#include "SkeletonImporter.h"
#include <assimp/scene.h>

bool SkeletonImporter::ImportSkeleton(const aiScene & scene, const aiMesh* file_path, const aiMatrix4x4& mesh_transformation, const std::string& output_file) const
{

	return true;
}